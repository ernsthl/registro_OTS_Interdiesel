services:
  - type: web
    name: control-ot
    env: python
    plan: free
    buildCommand: ""
    startCommand: streamlit run app.py --server.port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11